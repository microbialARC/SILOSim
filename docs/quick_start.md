
# Profiler
```
silosim profiler --input_genome /path/to/genome.fasta --species <species>
```

# Simulator
The simulator uses Profiler output to simulate bacterial evolution:

1. Run Profiler on your reference genome
2. Configure simulation parameters (mutation rates, recombination, gene gain/loss)
3. Run the simulator with the output from Profiler
